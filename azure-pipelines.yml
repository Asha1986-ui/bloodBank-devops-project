trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- task: Maven@4
  inputs:
    mavenPomFile: 'pom.xml'
    goals: 'clean package'

- script: |
    echo "Build completed successfully"
    echo "Skipping Docker build (handled in local/ACR step)"
  displayName: 'Build Stage'